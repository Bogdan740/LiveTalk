services:
  postgres:
    image: postgres:18.4
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: live_talk
    ports:
      - "5432:5432"
    tmpfs:
      - /tmp
